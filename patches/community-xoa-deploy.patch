diff --git a/@xen-orchestra/lite/scripts/xolite-loader.html b/@xen-orchestra/lite/scripts/xolite-loader.html
index 9d48835c7..f1ad24a16 100644
--- a/@xen-orchestra/lite/scripts/xolite-loader.html
+++ b/@xen-orchestra/lite/scripts/xolite-loader.html
@@ -7,23 +7,9 @@
     <title>XO Lite</title>
     <script>
       ;(async () => {
-        try {
-          // Try to load XO Lite from lite.xen-orchestra.com
-          const response = await fetch('https://lite.xen-orchestra.com/dist/index.html')
-          if (!response.ok) {
-            throw new Error(`Cannot download XO Lite from lite.xen-orchestra.com (${response.status})`)
-          }
-
-          document.open()
-          document.write(await response.text())
-          document.close()
-        } catch (err) {
-          console.log(err?.message ?? err)
-          // Fallback to local version of XO Lite
-          document.open()
-          document.write(await (await fetch('./xolite.html')).text())
-          document.close()
-        }
+        document.open()
+        document.write(await (await fetch('./xolite.html')).text())
+        document.close()
       })()
     </script>
   </head>
diff --git a/@xen-orchestra/lite/src/pages/xoa-deploy.vue b/@xen-orchestra/lite/src/pages/xoa-deploy.vue
index 26937a4d7..467225358 100644
--- a/@xen-orchestra/lite/src/pages/xoa-deploy.vue
+++ b/@xen-orchestra/lite/src/pages/xoa-deploy.vue
@@ -60,6 +60,11 @@
     <UiCard>
       <form @submit.prevent="deploy">
         <FormSection :label="t('configuration')">
+          <div class="row">
+            <VtsInputWrapper :label="t('xoa-image-url')">
+              <FormInput v-model="xoaImageUrl" :placeholder="XOA_DEFAULT_IMAGE_URL" />
+            </VtsInputWrapper>
+          </div>
           <div class="row">
             <VtsInputWrapper :label="t('storage')" :message="t('n-gb-required', { n: REQUIRED_GB })">
               <VtsSelect :id="srSelectId" accent="brand" />
@@ -69,10 +74,7 @@
             <VtsInputWrapper :label="t('network')">
               <VtsSelect :id="networkSelectId" accent="brand" />
             </VtsInputWrapper>
-            <VtsInputWrapper
-              :label="t('deploy-xoa-custom-ntp-servers')"
-              learn-more-url="https://docs.xen-orchestra.com/xoa#setting-a-custom-ntp-server"
-            >
+            <VtsInputWrapper :label="t('deploy-xoa-custom-ntp-servers')" learn-more-url="https://docs.xen-orchestra.com/xoa#setting-a-custom-ntp-server">
               <FormInput v-model="ntp" placeholder="xxx.xxx.xxx.xxx" />
             </VtsInputWrapper>
           </div>
@@ -124,7 +126,7 @@
               :label="t('admin-login')"
               learn-more-url="https://docs.xen-orchestra.com/xoa#default-xo-account"
             >
-              <FormInput v-model="xoaUser" required placeholder="email@example.com" />
+              <FormInput v-model="xoaUser" placeholder="admin@admin.net" disabled />
             </VtsInputWrapper>
           </div>
           <div class="row">
@@ -132,13 +134,13 @@
               :label="t('admin-password')"
               learn-more-url="https://docs.xen-orchestra.com/xoa#default-xo-account"
             >
-              <FormInput v-model="xoaPwd" type="password" required :placeholder="t('password')" />
+              <FormInput v-model="xoaPwd" placeholder="admin" disabled />
             </VtsInputWrapper>
             <VtsInputWrapper
               :label="t('admin-password-confirm')"
               learn-more-url="https://docs.xen-orchestra.com/xoa#default-xo-account"
             >
-              <FormInput v-model="xoaPwdConfirm" type="password" required :placeholder="t('password')" />
+              <FormInput v-model="xoaPwdConfirm" placeholder="admin" disabled />
             </VtsInputWrapper>
           </div>
         </FormSection>
@@ -149,27 +151,15 @@
           </div>
           <div class="row">
             <VtsInputWrapper :label="t('ssh-login')">
-              <FormInput value="xoa" placeholder="xoa" disabled />
+              <FormInput value="xo" placeholder="xo" disabled />
             </VtsInputWrapper>
           </div>
           <div class="row">
             <VtsInputWrapper :label="t('ssh-password')">
-              <FormInput
-                v-model="sshPwd"
-                type="password"
-                :placeholder="t('password')"
-                :disabled="!enableSshAccount"
-                :required="enableSshAccount"
-              />
+              <FormInput v-model="sshPwd" placeholder="xopass" disabled />
             </VtsInputWrapper>
             <VtsInputWrapper :label="t('ssh-password-confirm')">
-              <FormInput
-                v-model="sshPwdConfirm"
-                type="password"
-                :placeholder="t('password')"
-                :disabled="!enableSshAccount"
-                :required="enableSshAccount"
-              />
+              <FormInput v-model="sshPwdConfirm" placeholder="xopass" disabled />
             </VtsInputWrapper>
           </div>
         </FormSection>
@@ -215,6 +205,19 @@ import { useRouter } from 'vue-router'
 
 const REQUIRED_GB = 20
 
+// ── XOA image source & proxy ──────────────────────────────────────────────────
+// XOA_PROXY_PORT must match PORT in xoa-proxy.py (default 9001).
+// xoaImageUrl is user-editable in the Configuration section of the form.
+const XOA_DEFAULT_IMAGE_URL = 'https://xo-image.yawn.fi/downloads/image.xva.gz'
+const XOA_PROXY_PORT = 9001
+
+const xoaImageUrl = ref(XOA_DEFAULT_IMAGE_URL)
+
+/** Builds the local proxy URL that VM.import will pull from. */
+function buildProxyUrl(sourceUrl: string): string {
+  return `http://127.0.0.1:${XOA_PROXY_PORT}/image.xva?src=${encodeURIComponent(sourceUrl)}`
+}
+
 const { t } = useI18n()
 const router = useRouter()
 
@@ -307,12 +310,14 @@ const ip = ref('')
 const netmask = ref('')
 const dns = ref('')
 const gateway = ref('')
-const xoaUser = ref('')
-const xoaPwd = ref('')
-const xoaPwdConfirm = ref('')
+// Baked-in defaults — fields are disabled in the UI but bound via v-model
+// so deploy() guards read the correct values without any user input required.
+const xoaUser = ref('admin@admin.net')
+const xoaPwd = ref('admin')
+const xoaPwdConfirm = ref('admin')
 const enableSshAccount = ref(true)
-const sshPwd = ref('')
-const sshPwdConfirm = ref('')
+const sshPwd = ref('xopass')
+const sshPwdConfirm = ref('xopass')
 
 async function deploy() {
   if (selectedSr.value === undefined || selectedNetwork.value === undefined) {
@@ -332,7 +337,7 @@ async function deploy() {
 
   if (xoaUser.value === '' || xoaPwd.value === '') {
     // Should not happen
-    console.error('Missing XOA credentials')
+    console.error('Missing XOA user information')
     return
   }
 
@@ -344,7 +349,7 @@ async function deploy() {
 
   if (enableSshAccount.value && sshPwd.value === '') {
     // Should not happen
-    console.error('Missing XOA credentials')
+    console.error('Missing XOA ssh credentials')
     return
   }
 
@@ -359,9 +364,14 @@ async function deploy() {
   try {
     status.value = t('deploy-xoa-status:importing')
 
+    // xoa-proxy.py (running on Dom0) fetches XOA_SOURCE_URL via HTTPS,
+    // decompresses the .xva.gz on-the-fly, and serves the raw .xva over HTTP.
+    // XAPI's VM.import then consumes the plain HTTP stream directly.
+    const proxyUrl = buildProxyUrl(xoaImageUrl.value)
+
     vmRef.value = (
       (await xapi.call('VM.import', [
-        'http://xoa.io/xva',
+        proxyUrl,
         selectedSr.value.$ref,
         false, // full_restore
         false, // force
@@ -371,7 +381,10 @@ async function deploy() {
     status.value = t('deploy-xoa-status:configuring')
 
     const [vifRef] = (await xapi.call('VM.get_VIFs', [vmRef.value])) as string[]
-    await xapi.call('VIF.destroy', [vifRef])
+
+    if (vifRef !== undefined) {
+    	await xapi.call('VIF.destroy', [vifRef])
+    }
 
     if (!deploying.value) {
       return
diff --git a/@xen-orchestra/lite/xoa-proxy/deploy-proxy.sh b/@xen-orchestra/lite/xoa-proxy/deploy-proxy.sh
new file mode 100755
index 000000000..af822bbed
--- /dev/null
+++ b/@xen-orchestra/lite/xoa-proxy/deploy-proxy.sh
@@ -0,0 +1,75 @@
+#!/usr/bin/env bash
+# deploy-proxy.sh
+# ════════════════════════════════════════════════════════════════════════════
+# Installs xoa-proxy.py on the XCP-ng Dom0 host and (optionally) builds +
+# deploys the updated XO-Lite bundle.
+#
+# Usage:
+#   ./deploy-proxy.sh                          # uses HOST from environment
+#   ./deploy-proxy.sh root@192.168.0.85        # explicit host
+#   ./deploy-proxy.sh root@192.168.0.85 --app  # also build & deploy XO-Lite
+#
+# Requirements on your dev machine: ssh, rsync, (npx yarn if using --app)
+# ════════════════════════════════════════════════════════════════════════════
+set -euo pipefail
+
+# ── Config ────────────────────────────────────────────────────────────────────
+HOST="${1:-${XO_HOST:-root@192.168.0.85}}"
+DEPLOY_APP=false
+
+# Parse optional flags
+for arg in "$@"; do
+  [[ "$arg" == "--app" ]] && DEPLOY_APP=true
+done
+
+XOLITE_DIR="$(cd "$(dirname "$0")/.." && pwd)"  # project root (one level up from xoa-proxy/)
+PROXY_SCRIPT="$(dirname "$0")/xoa-proxy.py"
+SERVICE_FILE="$(dirname "$0")/xoa-proxy.service"
+REMOTE_WWW="/opt/xensource/www"
+REMOTE_SERVICE="/etc/systemd/system/xoa-proxy.service"
+
+echo "▶ Target host : $HOST"
+echo "▶ Deploy app  : $DEPLOY_APP"
+echo ""
+
+# ── Step 1: Install the proxy script on Dom0 ──────────────────────────────────
+echo "── [1/3] Copying xoa-proxy.py to $HOST:$REMOTE_WWW/"
+scp "$PROXY_SCRIPT" "$HOST:$REMOTE_WWW/"
+
+# ── Step 2: Install and enable the systemd service ────────────────────────────
+echo "── [2/3] Installing systemd unit"
+scp "$SERVICE_FILE" "$HOST:$REMOTE_SERVICE"
+
+#ssh "$HOST" bash <<'REMOTE'
+#  set -e
+#  systemctl daemon-reload
+#  systemctl enable xoa-proxy
+#  # Restart if already running, start if not
+#  if systemctl is-active --quiet xoa-proxy; then
+#    systemctl restart xoa-proxy
+#    echo "  ✔ xoa-proxy restarted"
+#  else
+#    systemctl start xoa-proxy
+#    echo "  ✔ xoa-proxy started"
+#  fi
+#  # Verify it came up
+#  sleep 1
+#  systemctl is-active xoa-proxy && echo "  ✔ xoa-proxy is running" || echo "  ✘ xoa-proxy failed to start — check: journalctl -u xoa-proxy"
+#REMOTE
+
+# ── Step 3 (optional): Build and deploy XO-Lite ───────────────────────────────
+if [[ "$DEPLOY_APP" == "true" ]]; then
+  echo "── [3/3] Building XO-Lite"
+  cd "$XOLITE_DIR" && npx yarn build
+
+  echo "      Deploying dist/ to $HOST:$REMOTE_WWW/"
+  cd "$XOLITE_DIR/dist"
+  scp -r * "$HOST:$REMOTE_WWW/"
+  echo "  ✔ XO-Lite deployed"
+else
+  echo "── [3/3] Skipping XO-Lite build (pass --app to include)"
+fi
+
+echo ""
+echo "✔ Done. Proxy endpoint: http://127.0.0.1:9001/image.xva?src=<https-url>"
+echo "  Logs: ssh $HOST journalctl -u xoa-proxy -f"
diff --git a/@xen-orchestra/lite/xoa-proxy/xoa-proxy.py b/@xen-orchestra/lite/xoa-proxy/xoa-proxy.py
new file mode 100644
index 000000000..cec06f73b
--- /dev/null
+++ b/@xen-orchestra/lite/xoa-proxy/xoa-proxy.py
@@ -0,0 +1,282 @@
+#!/usr/bin/env python3
+# -*- coding: utf-8 -*-
+
+import argparse
+import gzip
+import http.server
+import logging
+import os
+import signal
+import socketserver
+import ssl
+import sys
+import threading
+import urllib.error
+import urllib.request
+from urllib.parse import parse_qs, urlparse
+
+# ── Configuration ──────────────────────────────────────────────────────────────
+PORT = 9001          # Avoids clash with Vite dev (3000), XAPI (443/80)
+BIND = "127.0.0.1"  # Loopback only — XAPI is on the same host, no external exposure needed
+CHUNK_SIZE = 64 * 1024  # 64 KiB — large enough to keep the pipeline fed, small enough to stay lean
+
+# Populated by main() after CLI/env parsing; read by XoaProxyHandler._stream_xva().
+SSL_VERIFY: bool = True
+
+# A non-blocking lock that ensures only one XVA stream is active at a time.
+# A second concurrent request receives HTTP 409 immediately.
+_import_lock = threading.Lock()
+
+
+# ── Server ─────────────────────────────────────────────────────────────────────
+
+class XoaProxyServer(socketserver.ThreadingMixIn, http.server.HTTPServer):
+    """Threaded HTTP server — each request runs in its own daemon thread.
+
+    daemon_threads=True means worker threads are killed automatically when the
+    main thread exits, so Ctrl+C / SIGTERM shuts down instantly regardless of
+    whether a multi-minute stream is in progress.
+    Concurrent imports are intentionally blocked via _import_lock.
+    """
+    daemon_threads = True
+    allow_reuse_address = True
+
+logging.basicConfig(
+    level=logging.INFO,
+    format="[xoa-proxy] %(asctime)s %(levelname)s %(message)s",
+    datefmt="%H:%M:%S",
+)
+log = logging.getLogger("xoa-proxy")
+
+
+# ── Request handler ────────────────────────────────────────────────────────────
+
+class XoaProxyHandler(http.server.BaseHTTPRequestHandler):
+    """Handles a single GET /image.xva?src=<url> request."""
+
+    server_version = "xoa-proxy/1.0"
+    sys_version = ""  # Suppress Python version from the Server response header
+
+    # ── Routing ────────────────────────────────────────────────────────────────
+
+    def do_GET(self) -> None:
+        parsed = urlparse(self.path)
+
+        if parsed.path != "/image.xva":
+            self._error(404, f"Unknown path '{parsed.path}'. Expected /image.xva")
+            return
+
+        params = parse_qs(parsed.query)
+        src_list = params.get("src")
+
+        if not src_list:
+            self._error(400, "Missing required query parameter: src")
+            return
+
+        src_url = src_list[0]
+
+        if not src_url.startswith("https://"):
+            self._error(400, f"src must start with https://, got: {src_url[:40]}")
+            return
+
+        self._stream_xva(src_url)
+
+    # ── Core pipeline ──────────────────────────────────────────────────────────
+
+    def _stream_xva(self, src_url: str) -> None:
+        """
+        Fetch src_url (HTTPS .xva.gz), decompress, stream as HTTP to XAPI.
+
+        Only one import may run at a time — a second concurrent request receives
+        HTTP 409 Conflict immediately. HTTP/1.0 semantics: no Content-Length
+        (unknown after decompression), connection closed when done. libcurl
+        (used internally by VM.import) reads until EOF — spec-compliant.
+        """
+        if not _import_lock.acquire(blocking=False):
+            self._error(409, "An import is already in progress. Only one concurrent import is allowed.")
+            return
+
+        try:
+            self._do_stream(src_url)
+        finally:
+            _import_lock.release()
+
+    def _do_stream(self, src_url: str) -> None:
+        """Inner stream logic — called only when the import lock is held."""
+        log.info("Starting stream  src=%s", src_url)
+
+        # Build the SSL context according to the --no-verify-ssl flag.
+        # Verification is ON by default; disable only for self-signed certs.
+        if SSL_VERIFY:
+            # Uses the host's system CA bundle (/etc/pki/tls/certs/ on XCP-ng).
+            ssl_ctx = ssl.create_default_context()
+        else:
+            ssl_ctx = ssl.create_default_context()
+            ssl_ctx.check_hostname = False
+            ssl_ctx.verify_mode = ssl.CERT_NONE
+            log.warning("SSL certificate verification DISABLED — self-signed cert mode")
+
+        req = urllib.request.Request(
+            src_url,
+            headers={
+                "User-Agent": "xoa-lite-proxy/1.0",
+                # Tell the upstream not to apply an extra layer of HTTP-level
+                # gzip compression on top of the already-.gz file content.
+                "Accept-Encoding": "identity",
+            },
+        )
+
+        try:
+            with urllib.request.urlopen(req, context=ssl_ctx) as upstream:
+                log.info(
+                    "Upstream connected  status=%d content-length=%s content-type=%s",
+                    upstream.status,
+                    upstream.headers.get("Content-Length", "unknown"),  # log expected size
+                    upstream.headers.get("Content-Type", "unknown"),
+                )
+
+                self.send_response(200)
+                self.send_header("Content-Type", "application/octet-stream")
+                self.send_header("Connection", "close")
+                self.end_headers()
+
+                bytes_sent = 0
+                stream_complete = False  # ← track clean completion
+
+                try:
+                    with gzip.GzipFile(fileobj=upstream) as gz:
+                        while True:
+                            chunk = gz.read(CHUNK_SIZE)
+                            if not chunk:
+                                break
+                            self.wfile.write(chunk)
+                            bytes_sent += len(chunk)
+                    # If we exit the `with gz` block without exception,
+                    # the gzip footer was validated — stream is complete.
+                    stream_complete = True
+
+                except EOFError as exc:
+                    # gzip stream ended before the footer — upstream truncated
+                    log.error(
+                        "Upstream truncated mid-stream after %.1f MiB: %s",
+                        bytes_sent / 1024 ** 2,
+                        exc,
+                    )
+                    # Connection is already open to XAPI — close it abruptly
+                    # so XAPI gets a connection reset rather than a clean EOF,
+                    # making the failure mode more explicit on the XAPI side.
+                    return
+
+                except OSError as exc:
+                    log.error("Gzip decompression error after %.1f MiB: %s", bytes_sent / 1024 ** 2, exc)
+                    return
+
+                finally:
+                    if stream_complete:
+                        log.info(
+                            "Stream complete  bytes_sent=%d (%.1f MiB)",
+                            bytes_sent,
+                            bytes_sent / 1024 ** 2,
+                        )
+                    else:
+                        log.error(
+                            "Stream INCOMPLETE  bytes_sent=%d (%.1f MiB) — "
+                            "XAPI will receive premature EOF and fail with INTERNAL_ERROR",
+                            bytes_sent,
+                            bytes_sent / 1024 ** 2,
+                        )
+
+        except urllib.error.URLError as exc:
+            log.error("Upstream fetch failed: %s", exc)
+            self._error(502, f"Failed to fetch upstream image: {exc}")
+
+        except BrokenPipeError:
+            log.warning("Client (XAPI) closed connection before stream completed")
+
+        except Exception as exc:
+            log.exception("Unexpected error during stream: %s", exc)
+
+    # ── Helpers ────────────────────────────────────────────────────────────────
+
+    def _error(self, code: int, detail: str) -> None:
+        """Send a plain-text HTTP error response."""
+        body = (detail + "\n").encode()
+        self.send_response(code)
+        self.send_header("Content-Type", "text/plain; charset=utf-8")
+        self.send_header("Content-Length", str(len(body)))
+        self.end_headers()
+        self.wfile.write(body)
+        log.warning("HTTP %d: %s", code, detail)
+
+    def log_message(self, fmt: str, *args) -> None:  # type: ignore[override]
+        """Suppress the default per-request access log line (we log ourselves)."""
+        pass
+
+
+# ── Server bootstrap ───────────────────────────────────────────────────────────
+
+def _parse_args() -> argparse.Namespace:
+    parser = argparse.ArgumentParser(
+        description="XOA Deploy Proxy — HTTPS+gunzip bridge for XAPI VM.import",
+        formatter_class=argparse.RawDescriptionHelpFormatter,
+        epilog=(
+            "Environment variables (override defaults, CLI flags take precedence):\n"
+            "  XOA_PROXY_NO_VERIFY_SSL=1   Disable SSL certificate verification\n"
+            "  XOA_PROXY_PORT=<n>          Listening port (default: 9001)\n"
+            "  XOA_PROXY_BIND=<addr>       Bind address  (default: 127.0.0.1)\n"
+        ),
+    )
+    parser.add_argument(
+        "--no-verify-ssl",
+        action="store_true",
+        default=os.environ.get("XOA_PROXY_NO_VERIFY_SSL", "0") not in ("", "0", "false", "no"),
+        help=(
+            "Disable upstream TLS certificate verification. "
+            "Use when the XOA image is hosted behind a self-signed certificate. "
+            "Also enabled by setting XOA_PROXY_NO_VERIFY_SSL=1."
+        ),
+    )
+    parser.add_argument(
+        "--port",
+        type=int,
+        default=int(os.environ.get("XOA_PROXY_PORT", PORT)),
+        metavar="PORT",
+        help=f"Port to listen on (default: {PORT}, env: XOA_PROXY_PORT)",
+    )
+    parser.add_argument(
+        "--bind",
+        default=os.environ.get("XOA_PROXY_BIND", BIND),
+        metavar="ADDR",
+        help=f"Address to bind to (default: {BIND}, env: XOA_PROXY_BIND)",
+    )
+    return parser.parse_args()
+
+
+def main() -> None:
+    global SSL_VERIFY  # noqa: PLW0603
+
+    args = _parse_args()
+    SSL_VERIFY = not args.no_verify_ssl
+
+    server = XoaProxyServer((args.bind, args.port), XoaProxyHandler)
+
+    def _on_signal(sig: int, _frame) -> None:
+        log.info("Received signal %d, shutting down immediately", sig)
+        # sys.exit() kills the main thread instantly; daemon worker threads
+        # (including any in-progress stream) are reaped by the OS automatically.
+        sys.exit(0)
+
+    signal.signal(signal.SIGTERM, _on_signal)
+    signal.signal(signal.SIGINT, _on_signal)
+
+    log.info(
+        "Listening on http://%s:%d/image.xva?src=<https://...xva.gz>  ssl_verify=%s",
+        args.bind,
+        args.port,
+        SSL_VERIFY,
+    )
+    server.serve_forever()
+
+
+if __name__ == "__main__":
+    main()
diff --git a/@xen-orchestra/lite/xoa-proxy/xoa-proxy.service b/@xen-orchestra/lite/xoa-proxy/xoa-proxy.service
new file mode 100644
index 000000000..289458a98
--- /dev/null
+++ b/@xen-orchestra/lite/xoa-proxy/xoa-proxy.service
@@ -0,0 +1,53 @@
+[Unit]
+Description=XOA Deploy Proxy — HTTP/gunzip bridge for XAPI VM.import
+Documentation=https://github.com/vatesfr/xen-orchestra
+# Start after the network is up (we need outbound HTTPS to Vates CDN)
+After=network-online.target
+Wants=network-online.target
+
+[Service]
+Type=simple
+ExecStart=/usr/bin/python3 /opt/xensource/www/xoa-proxy.py
+Restart=on-failure
+RestartSec=5s
+
+# ── SSL configuration ────────────────────────────────────────────────────────
+# Certificate verification is ON by default (recommended for public Vates CDN).
+#
+# If your XOA image is hosted behind a self-signed certificate, choose ONE of:
+#
+#   Option A — CLI flag (edit ExecStart above):
+#     ExecStart=/usr/bin/python3 /opt/xensource/www/xoa-proxy.py --no-verify-ssl
+#
+#   Option B — Environment variable (uncomment the line below):
+#     Environment=XOA_PROXY_NO_VERIFY_SSL=1
+#
+# Other tunables (also settable via CLI, see --help):
+#   Environment=XOA_PROXY_PORT=9001
+#   Environment=XOA_PROXY_BIND=127.0.0.1
+
+# ── Logging ─────────────────────────────────────────────────────────────────
+# Output goes to journald: journalctl -u xoa-proxy -f
+StandardOutput=journal
+StandardError=journal
+
+# ── Minimal privilege hardening ──────────────────────────────────────────────
+# The proxy only needs outbound HTTPS and to listen on a loopback port > 1024.
+# No filesystem writes, no setuid, no new privileges.
+NoNewPrivileges=yes
+PrivateTmp=yes
+
+# Read-only access to the script itself; no write access to the rest of the FS.
+# (ProtectSystem=strict is not available on older CentOS 7-based XCP-ng builds;
+#  use 'full' as the safe fallback for XCP-ng 8.x)
+ProtectSystem=full
+ProtectHome=yes
+
+# Bind only to loopback — belt-and-suspenders with the Python-level BIND setting.
+# Note: RestrictAddressFamilies requires a kernel with seccomp. If the unit
+# fails to start, comment out the two lines below.
+RestrictAddressFamilies=AF_INET AF_INET6
+AmbientCapabilities=
+
+[Install]
+WantedBy=multi-user.target
diff --git a/@xen-orchestra/web-core/lib/locales/en.json b/@xen-orchestra/web-core/lib/locales/en.json
index c5bb9d357..89a120986 100644
--- a/@xen-orchestra/web-core/lib/locales/en.json
+++ b/@xen-orchestra/web-core/lib/locales/en.json
@@ -967,6 +967,7 @@
   "xoa-deploy-failed!": "Sorry, deployment failed!",
   "xoa-deploy-retry": "Try again to deploy XOA",
   "xoa-deploy-successful!": "XOA deployment successful!",
+  "xoa-image-url": "XOA Image URL",
   "xoa-ip": "XOA IP address",
   "xoa-password-confirm-different": "XOA password confirmation is different",
   "xoa-ssh-account": "XOA SSH account",
